<?php
// מוחק את נתוני ה-נתוני ההתחברות השמורים בשרת ומוציא את המשתמש מהמערכת
if ($action === "logout") {
 // מנהל את ה-נתוני ההתחברות השמורים בשרת של המשתמש המחובר.
    session_destroy();
    out(["ok" => true]);
}
